<div class="contenedor-formulario">

  <p class="alert-danger"><%= get_flash(@conn, :error) %></p>

  <%= form_for @changeset, usuario_path(@conn, :create), fn f -> %>
    <div class="toggle">
      <span><%= link "Registrar Comercio", to: comercio_path(@conn, :new), class: "link" %></span>
    </div>
    <h2>Datos del Usuario</h2>

    <%= text_input f, :nombres, [class: "text_input",placeholder: "Ingrese su nombre", required: ""] %>
    <%= text_input f, :apellido, [class: "text_input",placeholder: "Ingrese su apellido", required: ""] %>
    <%= email_input f, :email, [class: "email_input",placeholder: "Correo Electronico", required: ""] %>
    <%= password_input f, :password, [class: "password_input",placeholder: "Ingrese su contraseña", required: ""] %>
    <%= number_input f, :telefono, [class: "number_input",placeholder: "Ingrese su numero telefonico", required: ""] %>

    <%= submit "Guardar", [class: "button"] %>
  <%end%>

  <%= button "Volver", to: login_path(@conn, :login_usuario), class: "button-volver"%>
</div>
